@model List<Product>

@section Footer {
    <div>
        only for index footer
    </div>
}

<div class="row my-3">
    @foreach (var prd in Model)
    {
        <partial name="_ProductCard" model="@prd" />
    }
</div>